// Build Corpus now uses the canonical Ship Record schema-v2 batch engine.
// Kept at this path so the existing application shell and deployment checks remain stable.
import './batch-extraction-v2.js';
