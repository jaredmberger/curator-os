{
  "name": "curator-os",
  "version": "5.2.8",
  "private": true,
  "description": "Evidence-first institutional operating system for Ocean Liner Curator",
  "scripts": {
    "build": "node scripts/build.js",
    "validate": "node scripts/validate.js",
    "test:smoke": "node tests/smoke.test.js",
    "test:database": "node tests/database.test.js",
    "test": "npm run test:database && npm run test:smoke",
    "check": "npm run validate && npm run test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
