# Restore Findings dashboard

This branch restores the last known-good CuratorOS shell from immediately before the dashboard and grouped-navigation regression.

The repair restores the Findings landing dashboard, scan/index imports, scan history, metrics, filters, findings actions, and grouped sidebar headings.

The Page Studio toolbar link will be re-added separately after this repair is merged so the regression is not reintroduced.
